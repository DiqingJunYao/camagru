# test_project

Small Fastify server run via Docker.

Run (from project root):

```bash
make
```

Then open http://localhost:8443

Clean without Volume:

```bash
make down
```

Clean with Volume:

```bash
make down -v
```
